# Замечания по Jev — Validation 2A

Jev рассматривается только как атомарный семантический датчик по `Docs/jev_context.md`. Каталог R01–R10 во время разметки не менялся. Ниже — предложения перед отдельным следующим этапом, не интеграция.

## R01 — Ясность внутреннего решения

**Вердикт:** REVISE. Напрямую отдавать Jev сейчас: нет; после уточнения — `ordinal`.

**История/состояние:** Нет; брать текущий gate. **Логика в коде:** Нет.

**Минимальный вход:** Имя роли, её действие и срок/результат из одной актуальной реплики.

**Проблема разметки:** Граница между названным барьером и действующим шагом. Точное согласие 65.2%; содержательных расхождений 8/23.

**Правило после уточнения.** Уровень 2 требует одновременно: ответственную роль клиента, её конкретное действие и ограниченную контрольную точку. Разрозненные упоминания руководителя, звонка продавца и общей надежды на решение не складываются в уровень 2. Уровень 1 — названная роль или барьер без полного набора. Уровень 0 допустим лишь после содержательного обсуждения процесса решения, где процесс не назван; при отсутствии такого разговора нужен UNKNOWN. Если возникает новый gate, классифицировать актуальный gate на дату среза, сохранив ссылку на исходную реплику. Это адресует 8/23 содержательных расхождений и ход 2→1 в одной сделке.

## R02 — Состояние шага, принадлежащего клиенту

**Вердикт:** SPLIT_SEMANTIC_AND_CODE. Напрямую отдавать Jev сейчас: нет; после уточнения — `choice`.

**История/состояние:** Да: последнее клиентское обязательство и его версии. **Логика в коде:** Да: срок, факт выполнения и число просрочек.

**Минимальный вход:** Обещание, ответственный, контрольная дата, подтверждение выполнения и последующие переносы.

**Проблема разметки:** Статус последнего обещания требует истории и расчёта срока. Точное согласие 87.0%; содержательных расхождений 3/23.

**Минимальное состояние для Jev/NeuroROP.** Хранить список клиентских обязательств с полями `commitment_id`, `agreed_at`, `customer_owner`, `promised_action`, `due_at` либо ожидаемый результат, `replaces_id`, `completion_evidence_at`. Jev извлекает из новых сообщений, было ли обещание конкретным и что клиент сообщил о выполнении/переносе. Код выбирает последнее действующее обязательство, сравнивает `due_at` со временем среза и считает явно пропущенные сроки. Значение «неоднократно сорван» требует минимум двух подтверждённых пропусков без новой конкретной замены; отсутствие записи о выполнении само по себе не равно пропуску. Если срок или статус неизвестен, ставить UNKNOWN; «шаг не согласован» — только при достаточном охвате обсуждения. После нового обещания прежний статус остаётся в истории, а метка отражает текущий шаг. Так устраняется необходимость для Jev самому считать даты и помнить всю историю.

## R03 — Достаточность исходных данных клиента

**Вердикт:** REVISE. Напрямую отдавать Jev сейчас: нет; после уточнения — `ordinal`.

**История/состояние:** Текущая версия задачи. **Логика в коде:** Нет.

**Минимальный вход:** Заявленное применение, запрошенные и фактически полученные данные.

**Проблема разметки:** Нет общего порога достаточности материалов для конкретной задачи. Точное согласие 73.9%; содержательных расхождений 4/23.

## R04 — Предметность технического запроса

**Вердикт:** REVISE. Напрямую отдавать Jev сейчас: нет; после уточнения — `ordinal`.

**История/состояние:** Нет. **Логика в коде:** Нет.

**Минимальный вход:** Слова клиента об ограничении или вопросе и связь с проверяемым действием.

**Проблема разметки:** Разметчики расходятся между UNKNOWN и отсутствием предметного запроса. Точное согласие 73.9%; содержательных расхождений 1/23.

## R05 — Статус проверки технической применимости

**Вердикт:** REVISE. Напрямую отдавать Jev сейчас: нет; после уточнения — `choice`.

**История/состояние:** Да: текущий scope и последняя проверка. **Логика в коде:** Нет.

**Минимальный вход:** Конкретная задача, техническое заключение или результат релевантного теста.

**Проблема разметки:** Размыта граница нерешённого вопроса и обоснованной применимости. Точное согласие 69.6%; содержательных расхождений 5/23.

## R06 — Согласие клиента с конкретной конфигурацией

**Вердикт:** REVISE. Напрямую отдавать Jev сейчас: нет; после уточнения — `boolean`.

**История/состояние:** Да: актуальная конфигурация. **Логика в коде:** Нет.

**Минимальный вход:** Явное подтверждение клиента и версия состава решения.

**Проблема разметки:** Согласие клиента путают с подготовленной продавцом конфигурацией. Точное согласие 78.3%; содержательных расхождений 5/23.

## R07 — Самостоятельное содержательное обращение клиента

**Вердикт:** SPLIT_SEMANTIC_AND_CODE. Напрямую отдавать Jev сейчас: нет; после уточнения — `boolean`.

**История/состояние:** Нет. **Логика в коде:** Да: направление/инициатор события, если канал надёжен.

**Минимальный вход:** Инициированное клиентом событие и его покупательский смысл.

**Проблема разметки:** Инициатор и содержательность смешаны; ответы расходятся. Точное согласие 56.5%; содержательных расхождений 8/23.

## R08 — Сравнение альтернатив по явному критерию

**Вердикт:** REVISE. Напрямую отдавать Jev сейчас: нет; после уточнения — `boolean`.

**История/состояние:** Нет. **Логика в коде:** Нет.

**Минимальный вход:** Названная клиентом альтернатива и критерий из доступного сообщения.

**Проблема разметки:** Неодинаково трактуются реальная альтернатива и явный критерий. Точное согласие 69.6%; содержательных расхождений 7/23.

## R09 — Формальный шаг закупки со стороны клиента

**Вердикт:** REVISE. Напрямую отдавать Jev сейчас: нет; после уточнения — `ordinal`.

**История/состояние:** Да: стадия формального процесса. **Логика в коде:** Часть статусов документов может извлекаться структурно.

**Минимальный вход:** Подтверждённое действие закупок, юристов или финансирования со стороны клиента.

**Проблема разметки:** Формальный шаг слишком близок к концу и путается с планом продавца. Точное согласие 78.3%; содержательных расхождений 5/23.

## R10 — Явное текущее предпочтение клиента

**Вердикт:** REVISE. Напрямую отдавать Jev сейчас: нет; после уточнения — `choice`.

**История/состояние:** Да: последнее явное предпочтение. **Логика в коде:** Нет.

**Минимальный вход:** Слова клиента о текущем выборе и названном критерии.

**Проблема разметки:** Молчание и отсутствие предпочтения смешаны; критерий не всегда явный. Точное согласие 78.3%; содержательных расхождений 4/23.
